{
  "name": "agentic-sounds",
  "displayName": "Agentic Sounds",
  "description": "Alertas y notificaciones sonoras en tiempo real para agentes de IA",
  "version": "1.0.0",
  "publisher": "emeorg",
  "icon": "assets/icon.png",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Machine Learning"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "antigravityAlert.testSound",
        "title": "Agentic Sounds: Probar Sonido de Notificación"
      },
      {
        "command": "antigravityAlert.status",
        "title": "Agentic Sounds: Estado del Monitor"
      },
      {
        "command": "antigravityAlert.setVolume",
        "title": "Agentic Sounds: Ajustar Volumen de Notificaciones"
      },
      {
        "command": "antigravityAlert.soundboard",
        "title": "Agentic Sounds: Catálogo de Sonidos (Soundboard)"
      }
    ],
    "configuration": {
      "title": "Agentic Sounds",
      "properties": {
        "agenticSounds.volume": {
          "type": "integer",
          "default": 100,
          "minimum": 0,
          "maximum": 100,
          "description": "Nivel de volumen (0 a 100) para las alertas y notificaciones sonoras."
        },
        "agenticSounds.cooldownMs": {
          "type": "integer",
          "default": 250,
          "minimum": 0,
          "maximum": 5000,
          "description": "Tiempo mínimo de espera (en milisegundos) entre sonidos idénticos para evitar saturación por ráfagas."
        },
        "agenticSounds.maxInactiveHours": {
          "type": "integer",
          "default": 24,
          "minimum": 1,
          "maximum": 720,
          "description": "Tiempo máximo (en horas) antes de limpiar de memoria los archivos de registro inactivos."
        }
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/emeorg/agentic-sounds.git"
  },
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "bun build ./src/extension.ts --outfile=./dist/extension.js --target=node --format=cjs --external=vscode --minify",
    "package": "bun run clean && bun run build && vsce package --no-dependencies --allow-missing-repository --allow-star-activation"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.8.0",
    "@types/vscode": "1.85.0",
    "@vscode/vsce": "^3.9.1",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3"
  }
}
